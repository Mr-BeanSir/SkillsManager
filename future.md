# Future Work

## Repository Sources

- Authenticated and private repository sources need a credential model before implementation. Decide where tokens are stored, how they are scoped per host/source, how they are redacted from UI/errors/logs, and whether Tauri should use the OS credential store before adding private GitHub/GitLab or private raw URL support.

## Phase 5 Follow-Ups

- Deferred `Phase 5 Task 5.1` and `Task 5.2` items now live in `future/phase5-followups.md`.
